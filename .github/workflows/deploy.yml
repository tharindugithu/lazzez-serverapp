name: Deploy Express App

on:
  push:
    branches:
      - master  # Change to your default branch if it's not 'main'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install SSH Key
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > private_key
        chmod 600 private_key
        ssh-keyscan ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

    - name: Deploy to VPS
      env:
        SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        SSH_HOST: ${{ secrets.SSH_HOST }}
        SSH_USERNAME: ${{ secrets.SSH_USERNAME }}
      run: |
        scp -i private_key -r ./* $SSH_USERNAME@$SSH_HOST:/root/lazzez-serverapp
        ssh -i private_key $SSH_USERNAME@$SSH_HOST "
          cd /root/lazzez-serverapp &&
          npm install &&
          pm2 restart lazeez-backend || pm2 start server.js --name lazeez-backend
        "
